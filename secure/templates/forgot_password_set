<!doctype html><html><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Forgot password</title><script src="https://cdn.tailwindcss.com"></script></head>
<body class="min-h-screen bg-slate-950 text-slate-100"><main class="max-w-3xl mx-auto p-6">
<div class="max-w-md mx-auto p-6 bg-slate-900/40 rounded-xl shadow-lg">
  <h1 class="text-xl font-semibold mb-4">Forgot password</h1>
  <p class="text-sm text-slate-300 mb-4">Enter your account email. If it exists, we’ll email a 6-digit code.</p>
  <form method="post" action="{% url 'forgot' %}" class="space-y-4">
    {% csrf_token %}
    <div>
      <label class="block text-sm mb-1" for="{{ form.email.id_for_label }}">Email</label>
      {{ form.email }}
      {% if form.email.errors %}<p class="mt-1 text-xs text-red-400">{{ form.email.errors|striptags }}</p>{% endif %}
    </div>
    <button class="w-full py-2 rounded-lg bg-emerald-600 hover:bg-emerald-700 text-sm font-medium">Send code</button>
  </form>
  <p class="text-xs mt-3"><a class="underline" href="{% url 'login' %}">Back to sign in</a></p>
</div>
</main></body></html>
